<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aliasadi.leak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="aliasadi.leak.asynctask.TaskBestPractical" />
        <activity android:name="aliasadi.leak.asynctask.inner.InnerTaskFixed" />
        <activity android:name="aliasadi.leak.asynctask.inner.InnerTaskLeak" />
        <activity android:name="aliasadi.leak.asynctask.StaticTaskLeak" />
        <activity android:name="aliasadi.leak.asynctask.StaticTaskFixed" />
        <activity android:name="aliasadi.leak.thread.InnerThreadLeak" />
        <activity android:name="aliasadi.leak.thread.InnerThreadFixed" />
        <activity android:name="aliasadi.leak.handler.InnerHandlerFixed" />
        <activity android:name="aliasadi.leak.singleton.ActivitySingleton" />
    </application>

</manifest>