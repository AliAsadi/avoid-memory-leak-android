<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aliesaassadi.memoryleak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AsyncTask.AsyncTaskBestPractical.AsyncTaskBestPractical" />
        <activity android:name=".AsyncTask.innerAsyncTaskLeakFixed" />
        <activity android:name=".AsyncTask.innerAsyncTaskLeak" />
        <activity android:name=".AsyncTask.StaticAsyncTaskLeak" />
        <activity android:name=".AsyncTask.StaticAsyncTaskLeakFixed" />
        <activity android:name=".Thread.innerThreadLeak" />
        <activity android:name=".Thread.innerThreadLeakFixed" />
        <activity android:name=".Handler.innerHandlerLeakFixed" />
        <activity android:name=".Singleton.ActivitySingletonLeak" />
    </application>

</manifest>